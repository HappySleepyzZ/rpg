# 260515 RootMotion 闪避会话记忆

## 项目和用户偏好

- 正式项目路径：`G:\ue5_projects\rpg\rpg260514`
- UE 版本：5.7
- UE 安装路径：`G:\Program Files\Epic Games\UE_5.7`
- 用户偏好：中文解释，代码里可以多写中文注释，架构要可维护，调试参数尽量暴露到 UE 编辑器，不要写死。
- 重要协作规则：一个会话只维护自己的 session memory，不要修改其它会话的 memory 文件。
- 重要协作规则：不要在未获得用户明确同意的情况下删除任何文件。

## 当前项目状态

- 输入系统使用 Enhanced Input，不混用旧版 `AxisMapping / ActionMapping`。
- 玩家蓝图：`Content/Characters/Player/BP_PlayerCharacter.uasset`
- 玩家角色 C++：`Source/rpg260514/Character/RPGPlayerCharacter.*`
- 玩家可 WASD 移动、鼠标转镜头、滚轮缩放、Shift 闪避/翻滚。
- 玩家 Mesh 使用 Manny，动画蓝图使用官方 `ABP_Unarmed`。

## 本会话完成内容

- 导入并提交 FreeSampleAnimationSet 相关资源。
- 新增 Git LFS 规则 `.gitattributes`，跟踪 UE 二进制资产。
- 新增 `IA_Sprint` 和 `IA_Zoom` 输入资产。
- 移除早期单节点 BlendSpace 驱动，保留蓝图里的官方 `ABP_Unarmed`。
- 在 `ARPGPlayerCharacter` 中加入可调闪避参数：
  - `DashStrength`
  - `DashDuration`
  - `DashCooldown`
  - `bDashUseInputDirection`
  - `bDashUseRootMotionAnimation`
  - `DashAnimation`
  - `DashFallbackAnimation`
  - `DashAnimationBlendIn`
  - `DashAnimationBlendOut`
  - `DashAnimationPlayRate`
  - `DashAnimationSlotName`
- 闪避支持两种模式：
  - 普通模式：`bDashUseRootMotionAnimation = false`，使用 `LaunchCharacter` 控制位移。
  - RootMotion 模式：`bDashUseRootMotionAnimation = true`，不调用 `LaunchCharacter`，播放前把角色朝向闪避方向，让动画 RootMotion 控制位移。
- Review 后修复了几个低风险架构问题：
  - `StartSprint / StopSprint / SprintAction` 的 C++ 语义已改为 `StartDash / StopDash / DashAction`。
  - `SprintSpeed` 已移除，避免保留未使用参数。
  - `RPGPlayerController` 现在优先使用资产 `IMC_Exploration`；只有资产缺失且 `bUseRuntimeMappingFallback` 为 true 时才创建运行时兜底 IMC，避免两个输入事实来源重复生效。
  - `rpg260514.Build.cs` 的 include path 收窄到当前真实存在的 `Character` 和 `Core`。
  - `Config/DefaultEngine.ini` 的默认地图已改为 `/Game/Maps/Prototype001`。
  - 新增 `Scripts/create_prototype_map.py`，用于生成最小默认地图资产。
  - `.gitignore` 增加 `__pycache__/` 和 `*.pyc`。

## 当前闪避配置

- 当前 `BP_PlayerCharacter` 使用 RootMotion Roll 闪避。
- 当前 `DashAnimation` 指向：
  `/Game/FreeSampleAnimationSet/Animations/DashDodgeRollSet/Mannequin/RootMotion/Roll/A_Roll_IdleFwd.A_Roll_IdleFwd`
- 已通过 UE Python 把 `A_Roll_IdleFwd.enable_root_motion` 从 `False` 改为 `True`。
- 这一步很关键：如果动画资产没有启用 Root Motion 提取，Mesh 会相对胶囊滚出去，动画结束时看起来像镜头或角色瞬间回弹。
- 当前用于配置 Roll 闪避的脚本：
  `Scripts/configure_root_motion_dash_roll.py`
- 当前用于自动选择普通闪避动画的脚本：
  `Scripts/set_dash_animation_from_imported_assets.py`

## 验证结果

- UE 5.7 构建命令已通过：
  `Build.bat rpg260514Editor Win64 Development -Project=G:\ue5_projects\rpg\rpg260514\rpg260514.uproject -WaitMutex -FromMsBuild`
- Review 修复后再次通过 UE 5.7 编译：
  `Build.bat rpg260514Editor Win64 Development G:\ue5_projects\rpg\rpg260514\rpg260514.uproject -WaitMutex -NoHotReload`
- `Scripts/create_prototype_map.py` 曾因 commandlet 下 SpawnActor 崩溃；脚本已收敛为只创建/保存最小地图，随后运行成功。
- 修复后已启动项目验证，`UnrealEditor.exe` 成功运行。
- 最新启动日志中没有 `Missing Modules`、`could not be compiled` 或项目模块加载失败。

## 后续注意事项

- 如果用户要调整翻滚动作手感，优先在 `BP_PlayerCharacter -> Movement|Dash` 中调：
  - `DashAnimationPlayRate`
  - `DashCooldown`
  - `DashAnimationBlendIn`
  - `DashAnimationBlendOut`
- RootMotion 模式下，`DashStrength` 和 `DashDuration` 基本不控制位移距离；位移主要来自动画根运动。
- 如果以后要改 RootMotion 距离，考虑新增可调的 RootMotion 缩放方案，或制作 In-Place 版本动画再用 `LaunchCharacter` 控制距离。
- 以后每次改完 UE bug，按用户要求要帮忙启动项目验证。

## Review 独立记录

本轮用户 review 提出的问题：

- P1：Git LFS 没完全生效，很多 `.uasset/.umap` 已作为普通 Git blob 入库。
- P1：`Content/Mannequins` 和 `Content/Characters/Mannequins` 完整重复。
- P1：默认地图仍是引擎模板 `/Engine/Maps/Templates/OpenWorld`，不是项目可玩地图。
- P2：输入映射有两个事实来源：资产 `IMC_Exploration` 和运行时创建的 MappingContext。
- P2：Sprint 命名和实际行为不一致，实际是一次性 Dash。
- P2：Native constructor 中硬编码资产路径较多，和 BP/脚本配置重复。
- P3：`Build.cs` 提前暴露了 AI/Combat/Data/Inventory 等尚不存在的模块路径。

已完成的 review 修改：

- LFS 历史迁移：
  - 用户确认这是个人新仓库、无协作者，可以重写历史。
  - 执行过 `git lfs migrate import --fixup --everything`，但覆盖不全。
  - 随后执行显式迁移：
    `git lfs migrate import --everything --include="*.uasset,*.umap,*.ubulk,*.uexp,*.upack,*.fbx,*.blend,*.obj,*.wav,*.mp3,*.ogg,*.png,*.jpg,*.jpeg,*.tga,*.exr,*.psd"`
  - 迁移后 `git lfs ls-files` 为 223，和当前已跟踪 UE/媒体二进制资源数量一致。
  - `git lfs fsck` 通过。
  - 抽查 `Content/Characters/Mannequins/Meshes/SKM_Manny_Simple.uasset`：Git 对象为 133 字节 LFS pointer，实际 LFS size 为 15825101。
  - 迁移后曾出现工作区资产仍是 LFS pointer 文本，UE 报 `Invalid value for PACKAGE_FILE_TAG`；已用 `git lfs checkout` 恢复真实二进制资产，再用 `git add -u` 归一化索引。
- 重复 Mannequin 资源：
  - 用户明确同意删除重复资源，保留 `Content/Characters/Mannequins`，删除 `Content/Mannequins`。
  - 删除前检查：`/Game/Mannequins/` 精确引用无命中；代码和脚本引用都指向 `/Game/Characters/Mannequins`。
  - 删除后检查：`Content/Characters/Mannequins` 仍为 128 个文件，`Content/Mannequins` 已不存在。
- 默认地图：
  - `Config/DefaultEngine.ini` 的 `GameDefaultMap` 改为 `/Game/Maps/Prototype001`。
  - 新增 `Content/Maps/Prototype001.umap`。
  - 新增 `Scripts/create_prototype_map.py`。脚本最初尝试 SpawnActor，UE 5.7 commandlet 下崩溃；已收敛为只创建/保存最小地图资产。
- 输入映射：
  - `RPGPlayerController` 优先使用资产 `IMC_Exploration`。
  - 新增 `bUseRuntimeMappingFallback`，仅在资产 IMC 缺失时创建运行时兜底 MappingContext。
- Dash 命名：
  - C++ 中 `StartSprint / StopSprint / SprintAction` 改为 `StartDash / StopDash / DashAction`。
  - 移除未使用的 `SprintSpeed`。
  - 资产文件暂时仍沿用 `IA_Sprint`，代码注释说明语义是 Dash，避免本轮额外重命名二进制资产。
- Build.cs：
  - `PublicIncludePaths` 收窄到当前真实存在的 `rpg260514`、`rpg260514/Character`、`rpg260514/Core`。
- 辅助脚本：
  - 新增 `Scripts/verify_core_assets.py`，用于加载 `BP_PlayerCharacter`、Manny Mesh、`ABP_Unarmed`、`MM_Dash` 和 RootMotion Roll，并确认 `/Game/Mannequins` 已不存在。
  - `.gitignore` 增加 `__pycache__/` 和 `*.pyc`。

Review 后验证结果：

- `Build.bat rpg260514Editor Win64 Development G:\ue5_projects\rpg\rpg260514\rpg260514.uproject -WaitMutex -NoHotReload` 编译通过。
- `Scripts/create_prototype_map.py` 最小地图生成脚本执行成功。
- `Scripts/verify_core_assets.py` 在删除重复 Mannequin 资源后执行成功，`Success - 0 error(s)`。
- LFS 历史迁移、`git lfs checkout`、`git add -u` 归一化后，再次执行 `Scripts/verify_core_assets.py` 成功，`Success - 0 error(s)`。
- 已按用户要求启动 UE 编辑器验证，`UnrealEditor` 进程正常响应。
- 当前工作区在最后检查时为干净状态。

Review 相关提交状态：

- 历史已重写，当前本地主线提交：
  - `38d0e77 docs: record lfs migration`
  - `634ed1a fix: address prototype review findings`
  - `46aeb46 feat: add root motion dash prototype`
  - `04be449 初始提交：项目骨架、文档和基础角色代码`
- 因为 LFS 迁移重写了历史，`origin/main` 与本地 `main` 分叉；后续推送需要：
  `git push --force-with-lease origin main`
- Git for Windows 偶发 `sh.exe couldn't create signal pipe, Win32 error 5`；遇到时可提权重跑对应 Git 命令。

尚未完全解决的 review 事项：

- Native constructor 里仍有 `ConstructorHelpers::FObjectFinder` 硬编码资产路径。当前只做了语义/输入/地图/LFS/重复资源的收敛；更彻底的方案是后续把默认资产迁到 BP/DataAsset/配置脚本，只让 C++ 定义能力。
